export interface UserProps {
  id: number;
  first_name: string;
  last_name: string;
  email: string;
  avatar: string;
}

export interface UserFormProps {
  first_name: string;
  last_name: string;
  email: string;
}
